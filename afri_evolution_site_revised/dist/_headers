/*
  Netlify headers for security and caching.
  Place this file in your publish folder (dist/_headers) before deploying to Netlify.
  Adjust GA/analytics origins as needed when you enable analytics.
*/

/*
  Static assets: long cache + immutable
*/
/assets/*
  Cache-Control: public, max-age=31536000, immutable

/* HTML files: short cache so updates appear promptly after deploy */
/*.html
  Cache-Control: public, max-age=0, must-revalidate

/* Security headers applied to all routes */
/* Content-Security-Policy assumes fonts and assets are self-hosted. Add analytics origins later. */
/* form-action includes Formspree; change to exact origin if you use a custom subdomain */
/* NOTE: keep this line unbroken for Netlify */
/* */
  Content-Security-Policy: default-src 'self'; script-src 'self'; connect-src 'self'; style-src 'self' 'unsafe-inline'; font-src 'self'; img-src 'self' data:; frame-ancestors 'none'; base-uri 'self'; form-action 'self' https://formspree.io
  Referrer-Policy: no-referrer-when-downgrade
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  X-XSS-Protection: 1; mode=block
  Strict-Transport-Security: max-age=15552000; includeSubDomains; preload
  Permissions-Policy: geolocation=(), camera=(), microphone=()
